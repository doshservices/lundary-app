class AddressModel {
  String id, state, city, address, contact, deliverTo;
  AddressModel({
    this.id,
    this.address,
    this.city,
    this.contact,
    this.deliverTo,
    this.state,
  });
}
