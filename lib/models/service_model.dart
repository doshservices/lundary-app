class ServiceModel {
  String id, name, state, category, image;
  double price;
  ServiceModel({
    this.category,
    this.id,
    this.image,
    this.name,
    this.price,
    this.state,
  });
}
